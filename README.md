# YTVidBot

A Telegram bot that downloads YouTube videos or extracts audio and returns them to users.

## Quick start

1. Create and activate a Python 3.11+ virtual environment.
2. Install dependencies:

```bash
pip install -r requirements.txt
